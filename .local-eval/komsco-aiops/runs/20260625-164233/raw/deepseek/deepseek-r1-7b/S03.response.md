

```json
{
  "environment": "ocp",
  "riskLevel": "medium",
  "steps": [
    {
      "order": 1,
      "tool": "oc",
      "command": "oc get -n portal web-frontend status",
      "purpose": "Check if the web frontend service is healthy and has a valid status.",
      "expectedEvidence": "Service should be marked as 'healthy' in the cluster status output."
    },
    {
      "order": 2,
      "tool": "curl",
      "command": "curl -X GET http://localhost:8080/", 
      "purpose": "Verify if the frontend API endpoint is reachable and returns a successful response.",
      "expectedEvidence": "HTTP status code 2xx (e.g., 200) indicates successful connection to the endpoint."
    },
    {
      "order": 3,
      "tool": "oc",
      "command": "oc logs -n portal web-frontend | tail -n 100",
      "purpose": "Inspect logs for any error messages or issues that might be causing the 503 response.",
      "expectedEvidence": "Error messages in logs indicating a problem with the backend service."
    }
  ],
  "approvalRequired": false
}
```